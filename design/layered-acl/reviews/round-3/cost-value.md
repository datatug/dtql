# A3 scoped-stage review cost/value

Both reviewers received the identical frozen delta, without the other's findings. A separate reconciler classified the reviews and verified corrections. This is one-project evidence, not a universal ranking.

| Accepted-root measure | Astra | Opus |
|---|---:|---:|
| Accepted roots | 2 | 2 |
| Unique accepted roots | 1 | 1 |
| Accepted high/critical defects | 0 | 0 |
| Reported review seconds | 183.932 | 190.451 |
| Tooling-reported USD | Unavailable | $1.030099 |
| USD per accepted root | Unavailable | ~$0.515 |
| USD per unique accepted root | Unavailable | ~$1.030 |

Astra uniquely found the canonical-quote mismatch; Opus uniquely prompted a useful container/restored-leaf clarification. Both supplied complementary regression examples. Accepted roots are {canonical, regression, container clarification}; shared root is regression, so accepted-root Jaccard=1/3=33⅓%, and each review's accepted coverage overlap=1/2. Submitted findings (Astra2, Opus8) mix allegations, clarifications and test ideas; do not claim this percentage measures all submitted finding overlap.

Astra missed the explicit container explanation opportunity. Opus missed the canonical byte inconsistency and raised several missing-requirement claims already addressed by mandatory normalization, matcher extension and canonical merging. The reconciler rejected those claims while retaining useful vectors. An aggregate false-positive rate would obscure these mixed findings; the reconciliation provides exact dispositions instead.

Both contributed useful evidence here. Opus alone would have missed the actual canonical-artifact blocker. Astra added that correction plus useful unique tests; Opus broadened explanatory/test coverage. Continue role-dependent dual review for security-sensitive contract changes, but do not infer financial return for Astra without exposed monetary telemetry. A focused delta packet and one reconciler avoid repeating the full architecture audit.

Actual usage: Astra: 454,980 input tokens (412,032 cached, included), 3,702 output (reasoning included), total458,682. Opus core: 64,310 input including cache creation, 13,702 output including thinking, total78,012. Exposed Haiku helper: 44,379 input +16 output adds 44,395 tokens. Full Opus tooling cost includes core $0.985640 and helper $0.044459; it is list/API reporting, not a subscription invoice. Do not add the duplicate core modelUsage record to top-level usage again.

Exact reviewer/reconciler/verification metadata is in [metrics.json](metrics.json). Codex reviewer/reconciler USD remains unavailable, never zero; cached rereads and different harness input delivery prevent a controlled token-efficiency comparison. Disclosed Opus cost across A1, A2/S1 and A3 is $5.944710; that is workflow spend, not independent discoveries summed across repeated reviews.
