# A3 applied correction ledger

R3-01: regenerated the two policy canonical YAML artifacts with the specified double-quote choice. Local document-authoring harness checks exact bytes and YAML parse/serialize idempotence; this does not claim a DALgo canonical serializer implementation/test exists.

R3-02: added adjacent-exclude raw/canonical fixtures, adjacent-include canonical fixtures, unsorted/deduplicated expected canonical data, dotted repeated-star parent equivalence, unaffected apple under fourth exclusion, interior-star column denial, three-stage field restriction transport and procedure policy. T21 now names actual owner/hop read/write and pure callable checks. Normalization-before-evaluation and canonical merged output are repeated beside the algorithm; no new algorithm or precedence.

R3-03: 19 explicitly distinguishes projected structural ancestors, authorized restored leaf updates, whole-parent evidence and mutations touching excluded descendants. A structural ancestor is not an additional veto on a leaf update. Added projection, leaf-update and parent-replacement-removes-secret vectors. Ordinary action/row/other-owner restrictions still apply.

Rejected/optional suggestions: no fixed semantic stage maximum, mandatory cache, glob-language solver, general schema rename, new mask engine or replacement of semantic validation. New-profile glob behavior must not fall through to the legacy edge-only matcher; legacy fields remain unchanged. The actual dynamic list contract remains variable length with advertised finite operational budgets.

Validation actually performed: 30 structural JSON fixtures; four canonical YAML artifacts with exact prescribed quote style and local serialization idempotence. Scoped semantic vectors remain declarative implementation obligations. Frozen A3 input and original reviews remain unchanged. No MVP implementation.
