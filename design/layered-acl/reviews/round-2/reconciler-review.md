**The frozen A2 + S1 packet is not ready for implementation dispatch.** The architecture remains viable. Resolve the coordinator admission contract, sample selection diagnostics, revision privacy, and complete parent-object evidence before freezing the contracts. The other accepted changes are bounded interoperability, documentation, and test corrections.

This reconciliation uses the frozen inputs and sealed reviews only. It does not assess the author’s subsequent live corrections. “Accept” below means a recommendation for document changes, pending the established human sign-off.

| Root / reviewer finding | Classification | Assessed severity | Recommendation and rationale |
|---|---|---|---|
| Coordinator lifecycle — A2-01, O2-18 session-interface portion | Consensus, with substantially greater depth from Astra | High overall | **Accept.** Freeze trusted ingress mode, admitted operations, snapshot/lock ownership, nested participation and execution capability. The author’s proposed resolution is appropriate: ACL-enabled public handles reject dynamic `RunReadwriteTransaction` workers **before invoking them**; protected execution admits bounded, predeclared operations/batches. Internal trusted coordination may reuse the existing storage transaction. Preserve ACL-disabled legacy behavior. Opus independently identifies only the narrower “structural rejection” interface inconsistency. |
| Sample identities and reduction — A2-02, O2-06 | Consensus on collisions; complementary Astra finding on selection denial | High overall | **Accept.** Give selection/template diagnostics legal references without inventing sampled rows; include definitive selection denial in reduction; allocate collision-free row IDs and define cross-hop remapping. Add field-denied selection, route-denied selection, unavailable selection, empty selection and template `s1` goldens. Opus’s collision fix alone would leave the zero-row denial unrepresentable. |
| Revision privacy — A2-03 | Complementary | Medium | **Accept.** Separate internal owner-generation revisions from public document revisions/ETags. Withhold whole-set tokens that reveal private-only changes. Public-document CAS must preserve concurrent private edits without exposing their revisions. `07` and `15` currently recommend whole-set digests while public envelopes require revisions. |
| Package completion dependencies — A2-04 | Complementary | Medium | **Accept.** Separate scaffolding start dependencies from completion gates. WP4a/WP5a must integrate the shared WP2a implementation and whichever WP2b interfaces they actually consume before completion. Remove the circular-looking “G2/G3 API” dependency wording. |
| InGitDB grant-store attack — O2-01 | Incorrect/unsupported as presented | No supported high defect | **Reject the claimed exploit and mandatory new store.** `02`, `06` and `15` explicitly exclude hostile Go context construction from the trust boundary; `07` requires actual-requester owner authorization; `10` requires owner-scoped management grants. An OpenVaultDB administrator is not thereby authorized to construct arbitrary trusted Go contexts. WP4a must concretely wire and test its owner grant resolver, but the review does not establish that a separate manifest grant store is necessary. |
| Literal-dot attack — O2-02 | Incorrect/unsupported | No supported high defect | **Reject.** `03` already rejects literal dots in physical segments and requires structural normalization. S1 repeats rejection at normalization/evidence boundaries. Dotted policy patterns and concrete segment arrays are intentionally different representations. Retain negative normalization fixtures; do not redesign field encoding. |
| Unsupported policy constructs — O2-03 | Complementary, overstated severity/remedy | Medium | **Accept narrowly.** Make load-time handling of unsupported selectors explicit so an enabled owner cannot silently ignore rules it cannot enforce. Do not adopt the proposed blanket rejection of every document containing `truncate`: the packet deliberately permits representable, nonexecutable unrestricted truncate rules. Distinguish document validity from unsupported execution. |
| Private omission reason — O2-04 | Complementary | Medium | **Accept.** A non-admin must receive `not_authorized`/opaque coalescing rather than `private`, which reveals classification/existence. This implements the existing privacy promise. |
| Plan residual versus missing evidence — O2-05 | Complementary clarification | Low | **Accept a clarifying vector.** `04` already explicitly requires no-row-read plan and conditional/complete residuals. State that deliberately deferred plan obligations are restrictions, not `row_evidence_required` failures. This is not the claimed major reducer contradiction. |
| Evidence field lists — O2-07 | Complementary | Medium | **Accept.** Freeze the relationship between `resource.columns` and `requiredFields`, including response echo behavior. Prefer omitting/forbidding redundant columns for the evidence endpoint, or define their distinct meanings explicitly. The fixture currently contains different lists. |
| Missing-row remote writes — O2-08 | Complementary limitation clarification | Low | **Accept narrowly.** Explicitly document that an absent-row operation requiring remote absence/pre-image evidence is unsupported by this evidence shape. Do **not** categorically reject conditional inserts: candidate-only insert checks can be evaluated without absence evidence, and lower insertion checks remain authoritative. |
| Existing generation digest — O2-09(a) | Complementary | Medium | **Accept.** Reuse an existing validated immutable digest directory; reject an inconsistent collision. Test repeated identical content and crash recovery. |
| Generation retention — O2-09(b) | Optional | Future/operational | **Defer.** Retention/GC is useful operational work, not a demonstrated authorization failure. Do not introduce a cleanup subsystem as a freeze prerequisite. Git history storage and active-tree retention are different concerns. |
| Startup locking/read-only mount — O2-09(c) | Incorrect/unsupported in part | Low at most | **Reject the missing-lock claim:** `15` explicitly requires the existing lock for generation writes/ref updates. Clarify capability failure if startup requires materialization on an unwritable mount; a new committed-blob loader is not mandatory MVP scope. |
| Mandatory execution-gate bypass — O2-10 | Incorrect/unsupported | No supported security defect | **Reject.** `02` requires every mandatory policy to permit; `03` says unmatched bindings leave default deny. A second ungated policy cannot bypass the gate. S1 supplies a complete narrowing-policy example. Route-only documents are optional architecture expansion. |
| Batch transport mapping — O2-11 | Complementary | Medium | **Accept.** Keep the host-owned batch envelope, but freeze per-item IDs, revision preconditions, normalized operations and conflict/error mapping. A new generic batch protocol is unnecessary. |
| Strict query decoding — O2-12 | Complementary | Medium | **Accept.** Explicitly bind DTQL query and sample text to strict duplicate/alias/tag/document/depth/node handling before evaluation. General safe-deserialization language is insufficiently exact for parallel decoder work. |
| Nonreference adapter ownership — O2-13 | Complementary, proposed remedy too broad | Low | **Accept capability derivation and test ownership.** WP5b owns the registered adapter capability matrix; unsupported capabilities default false. Reject “no coordinator means static-only”: proven row-filter pushdown may be valid without the mutation coordinator. Gate each operation on the capabilities it actually requires. |
| Trusted parameter types — O2-14 | Complementary | Medium | **Accept.** Define the representation/comparison semantics of `now` and valid use of list-valued role/group parameters. Fixed-width canonical UTC encoding with the specified lexical comparison is a bounded option. Add type-error vectors. No fixture depends on time predicates. |
| Duplicate `allOf` — O2-15 | Optional | Low | **Defer simplification.** The packet explicitly explains conjunction and mandates equality. Redundancy costs validation but is not an unresolved contract. |
| Diagnostic grant table — O2-16 | Optional | Low | **Defer or consolidate editorially.** `04`, `06` and `10` already specify separate grants, attenuation and disclosure. A table may improve navigation; it is not a missing authority model. |
| Acceptance/HTTP cross-references — O2-17 | Complementary | Low | **Accept.** Reconcile all gates to T01–T21/V01–V08 after S1 and include the evidence route and authority in the HTTP binding table. |
| `allowed` equivalence — O2-18 schema portion | Incorrect/unsupported | No missing semantic contract | **Reject as a defect.** `contracts/README` semantic validator #5 expressly requires the reducers and equivalence from `15`. Additional JSON Schema enforcement is optional strengthening. |
| Complete embedded evidence — O2-19 | Optional; security allegation unsupported | Future | **Defer minimization.** Registered pure evaluators receiving private images inside the trusted process is the approved boundary. Public projection is separately restricted. The review demonstrates no bypass; field minimization must not undermine complete final-image assessment. |
| Complete parent-object evidence — AS-01 | Complementary; unique Astra finding | Medium | **Accept before freeze.** An ordinary projected parent may be recursively redacted; an authoritative evidence value cannot silently be partial. Reject parent-object evidence when its complete value is unauthorized, or request explicitly authorized descendants. Define safe rejection for composite query references. Test excluded children both present and absent. |
| New-mask whitespace — Astra supplemental note | Complementary precision | Low | **Accept.** New masks must consistently preserve or reject surrounding whitespace rather than accidentally inherit legacy trimming. Preserve existing `fields` compatibility. |
| Exclusion enforcement mechanism — OS-01 | Incorrect/unsupported as a missing design | No supported high defect | **Reject the high finding.** S1 already mandates recursive redaction, every affected descendant/removal, rejection of unsupported arrays and disabling unsafe enumerable shortcuts. Applying unmodified legacy code would violate explicit instructions. Preserve the reviewer’s concrete regression cases; do not require one specific leaf-expansion implementation. |
| S1 schema materialization — OS-02; schema portion of OS-06 | Complementary delivery requirement | Low | **Accept.** Materialize and validate policy/rule mask definitions, mutual exclusion, restriction/reference variants, reason codes and tightened mask grammar before final freeze. S1 explicitly assigns this to WP1; absence from the earlier frozen base schema is expected, not a newly discovered architectural contradiction. |
| Mask canonical ordering — OS-02 portion | Optional | Low | **Defer a new set convention.** Existing canonical rules preserve semantic-array order unless explicitly classified as sets. That is a valid current answer. If sorting/deduplication is chosen, state it and update canonical fixtures consistently. |
| Mask-literal diagnostics — OS-03 | Complementary clarification | Low | **Accept.** Explicitly apply policy-read/disclosure authority to exclusion literals in ordinary restrictions. Otherwise return a reference. Do not emit an include-only “simplification” that omits an enforced exclusion. Public policy text remains intentionally readable by authorized policy readers; no privacy-default decision needs reopening. |
| Unresolved collection outcome — OS-04 | Complementary clarification | Low | **Accept.** Use `ACL_ENFORCEMENT_UNSUPPORTED` for unresolved/unsupported collection forms and `ACL_COLLECTION_DENIED` for a resolved name failing the gate; add migration wording. The narrow root-collection scope is already chosen. |
| Deny-column UI default — OS-05 | Optional reinforcement; claimed conflict unsupported | Low | **Defer as a new decision.** Preserve the existing deny-column action’s explicit allow-list; provide explicitly authored masks with the already-required future-field warning. Test both after schema growth. The packet does not direct silently replacing the former with wildcard masks. |
| Name-mask versus field grammar — OS-06 remaining portion | Incorrect/unsupported | No missing grammar | **Reject the claimed ambiguity.** S1 explicitly says individual column segments use the name grammar and separately defines dotted field patterns. Schema changes are already counted above. |
| Legacy `subtree` behavior — OS-07 | Optional | Low | **Defer as a separate fix; retain regression coverage.** Legacy preservation and parent-prefix behavior are already explicit. Do not change legacy meaning when implementing exclusions. |
| T21 owner/hop coverage — OS-08 | Complementary | Low | **Accept.** Assign direct adapter, OpenVaultDB and DataTug remote cases explicitly. The intersection case should include different mandatory owners and alternatives within one policy. This is test ownership precision, not a new enforcement design. |

The accepted changes belong to these work packages:

- **WP1:** sample references/reduction; revision domains; evidence fields and complete-parent semantics; redaction; parameter types; host batch mapping; S1 schema/canonical fixtures; updated acceptance gates.
- **WP2a/b:** strict policy/query and mask parsing; explicit protected-load behavior; parameter types; trusted admitted-plan/mode interfaces; mandatory gate compilation.
- **WP3a/b:** bounded coordinator lifecycle; shared sample diagnostics; complete evidence versus projection checks; existing S1 exclusion obligations across reads, references and mutations.
- **WP4a/b:** validated digest reuse; document-versus-generation CAS; real coordinator integration and lifecycle/crash tests.
- **WP5a/b/c and WP6:** wire/redaction consistency; complete remote evidence; host batch mapping; capability derivation; absent-evidence limitations and propagated sample IDs.
- **WP7/8:** unchanged explicit-list authoring default plus mask warnings; refreshed diagnostic fixtures; assigned T21 owner/hop matrix; actual storage and browser acceptance.
- **WP0/9:** start/completion dependency corrections and explicit protected-profile compatibility/migration notes.

No fresh human architecture decision is necessary for these repairs. The author’s bounded callback rejection resolves A2-01 consistently with the approved narrow MVP. Sampling, public-by-default visibility, private admin-only access, remote readable-evidence requirements and unsupported native effects remain approved choices. Implementation dispatch still requires the established sign-off after corrected contracts are verified.

For the reviewer comparison, the following counting treats O2-09 as three roots, O2-18 as two, and supplemental OS-02 as schema delivery plus optional canonical ordering. OS-06’s schema portion duplicates OS-02; its grammar claim remains separately assessed. Astra’s supplemental whitespace recommendation counts as one low root.

| Measure | Astra | Opus |
|---|---:|---:|
| Main roots | 4 | 22 |
| Supplemental roots | 2 | 9 |
| Total roots | 6 | 31 |
| Accepted roots credited to reviewer | 6 | 17 |
| Unique accepted roots | 4 | 15 |
| Accepted high-severity contributions independently established | 2 | 0 |
| Critical findings accepted | 0 | 0 |

Two roots overlap: lifecycle/session structure and sample identity/reference integrity. Main Jaccard is **2/24 = 8.3%**, with smaller-review overlap **2/4 = 50%**. Including the supplement, Jaccard is **2/35 = 5.7%**, with smaller-review overlap **2/6 = 33.3%**. Across both reviewers there are **21 accepted, seven rejected and seven deferred roots**. “Accepted” includes low documentation clarifications, not just defects.

Opus’s participation in the two shared roots does not mean it independently found their high-severity substance: its contributions were the structural-interface wording and ID collision. Astra additionally found dynamic transaction admission and selection denial before any row exists.

**Opus alone likely would not have sufficed for this freeze.** It provided useful breadth in wire consistency, persistence idempotence, parser requirements and test ownership, but missed revision-domain privacy, completion dependencies, parent-object evidence completeness and the deeper coordinator/sample failures. Its principal false positives relied on disregarding already explicit trust, normalization, intersection or S1 enforcement protections. Astra’s smaller set had substantially better defect precision here. Astra missed several useful bounded interoperability and operational details raised by Opus.

| Telemetry | Astra main | Astra supplement | Opus main | Opus supplement |
|---|---:|---:|---:|---:|
| Executed model | gpt-6-astra | gpt-6-astra | claude-opus-5 | claude-opus-5 |
| Start UTC, 2026-09-08 | 14:59:04.522 | 15:10:48.743 | 14:59:26.257 | 15:10:15.037 |
| End UTC | 15:10:48.743 | 15:14:16.297 | 15:05:24.116 | 15:12:47.257 |
| Reported elapsed seconds | 704.226 | 196.184 | 357.859 | 152.219 |
| Input tokens | 1,655,540 | 312,723 | 2 uncached + 105,508 cache creation | 2 uncached + 111,827 cache creation |
| Cached input included above | 1,571,712 | 303,616 | 0 cache reads | 0 cache reads |
| Output, including reasoning | 13,847 | 3,956 | 25,029 | 10,300 |
| Core-model total tokens | 1,669,387 | 316,679 | 130,539 | 122,129 |
| Tooling-reported USD, including helper | Unavailable | Unavailable | $1.758049 | $1.457713 |

Astra’s supplemental timestamp interval is 207.554 seconds, which differs from its reported 196.184 seconds; retain this discrepancy rather than silently reconciling it. Astra totals are cumulative-context accounting, including cached rereads, and are not comparable to Opus’s input delivery as a controlled efficiency benchmark.

Opus’s combined reported cost is **$3.215762**: **$3.056595** for Opus and **$0.159167** for the exposed `claude-haiku-4-5-20251001` helper. The helper adds 159,019 tokens beyond the 252,668 core Opus tokens. These are tooling-reported list/API costs, **not subscription invoices**. Opus cost per accepted root is approximately **$0.189**, and per unique accepted root **$0.214**. Astra monetary comparisons are unavailable.

Reconciler timing, tokens and cost are not exposed to this agent and must be attached from parent harness telemetry; unknown cost is not zero.

For this project, retain an independent Astra review for security-boundary and executable-contract questions, with Opus supplying a broad contract/test checklist. Then use one reconciler and focused correction verification. The evidence supports that division of work here; it does not establish a universal model ranking.
