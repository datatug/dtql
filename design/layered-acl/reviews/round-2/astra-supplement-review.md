**Supplement verdict: ready with one bounded contract clarification.** The mask model fits the existing evaluator and preserves the requested collection/column inclusion and exclusion behavior. Overall readiness remains subject to my sealed main report.

| ID | Severity / dimension | Evidence and concrete failure | Recommended action / readiness |
|---|---|---|---|
| **AS-01** | **Medium — nested field masks and evidence completeness** | Supplement “Column mask” combines parent inclusion, recursive redaction and application to evidence coverage. The supplied `fields.go:79–99` matches parent prefixes: with include `address.*` and exclude `address.internal_*`, `allows("address")` is true while `allows("address.internal_code")` is false. Base C4 remote evidence, however, requires an authoritative value for each requested field and prohibits representing withheld data as absence. An evidence request for `["address"]` therefore cannot simply follow ordinary recursive projection: returning the complete object leaks excluded children; returning a redacted object as `{state:"present",value:...}` falsely represents the whole requested field as complete. This boundary needs an explicit rule beyond applying `allows` to the requested path. | Specify that **projectable parent**, **readable complete value**, and **permitted scalar/query reference** are different checks using the same parsed masks. Ordinary wildcard projection may redact descendants. Evidence for an object-valued parent must fail safely when its complete value is not authorized, or request explicitly authorized descendant paths instead; it must not return a partial object as complete evidence. Whole-object query references likewise require a defined safe rule or unsupported rejection. Add a golden evidence case for included `address` with excluded `address.internal_code`, including when that child is absent. **WP1/WP3/WP5c/WP6. Freeze this clarification before implementing evidence integration.** |

The supplement already addresses the principal implementation traps exposed by the source excerpt:

- `enumerable()` returns top-level names, including `address`, without proving that returning the entire object is safe. The explicit requirement to disable unsafe projection shortcuts and recursively redact is necessary and should remain.
- Existing `disallowedUpdates()` checks only the named update path. The supplement correctly requires checking affected descendants and removals, including unchanged excluded children touched by parent replacement.
- Intersecting each inclusion-minus-exclusion set preserves independent policy restrictions. Keeping exclusions inside the deciding rule also avoids introducing a competing precedence system.

The collection gate is suitably separate from hierarchical path specificity. Canonical owner collection names, application before schema enumeration, and rejection of unresolved nested/native targets make `User_*` and `*` excluding `sys_*` implementable without expanding native execution scope.

One additional parser regression is worth including without treating it as a separate architectural defect: `parseFieldPatterns` trims surrounding whitespace at source lines 38–43. New collection, procedure and field-mask parsing should explicitly preserve or reject such literals consistently; it should not accidentally inherit trimming that changes which canonical name matches. Preserve existing legacy `fields` behavior separately. Also update procedure-mask negative fixtures for the supplement’s newly rejected interior/multiple stars.

I read the complete supplement and verified SHA256 `1dc80f6ab3a7d7c40298d3d7af383cbdc3aad21a5020b2fae8af70b8f9a1244c`. I used only it and the previously reviewed base packet, made no edits, and ran no implementation tests. The source excerpt ends partway through `redactMap`, so I do not claim that the supplied excerpt establishes every existing projection path.
