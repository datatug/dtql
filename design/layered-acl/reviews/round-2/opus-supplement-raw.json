{"duration_api_ms":153075,"stop_reason":"end_turn","session_id":"cb299255-39ff-430b-b11a-a4e175134c2f","total_cost_usd":1.4577129999999998,"usage":{"input_tokens":2,"cache_creation_input_tokens":111827,"cache_read_input_tokens":0,"output_tokens":10300,"output_tokens_details":{"thinking_tokens":6217},"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":111827,"ephemeral_5m_input_tokens":0},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":10300,"cache_read_input_tokens":0,"cache_creation_input_tokens":111827,"cache_creation":{"ephemeral_5m_input_tokens":0,"ephemeral_1h_input_tokens":111827},"type":"message"}],"speed":"standard"},"modelUsage":{"claude-haiku-4-5-20251001":{"inputTokens":81838,"outputTokens":19,"cacheReadInputTokens":0,"cacheCreationInputTokens":0,"webSearchRequests":0,"costUSD":0.08193299999999999,"contextWindow":200000,"maxOutputTokens":32000,"thinkingTokens":0,"canonicalModel":"claude-haiku-4-5","provider":"firstParty","costBasis":"list"},"claude-opus-5":{"inputTokens":2,"outputTokens":10300,"cacheReadInputTokens":0,"cacheCreationInputTokens":111827,"webSearchRequests":0,"costUSD":1.3757799999999998,"contextWindow":1000000,"maxOutputTokens":64000,"thinkingTokens":6217,"canonicalModel":"claude-opus-5","provider":"firstParty","costBasis":"list"}},"permission_denials":[],"terminal_reason":"completed","fast_mode_state":"off","fast_mode_disabled_reason":"sdk_opt_in_required","subagent_stats":{"spawned":0,"requested":{"background":0,"foreground":0,"unset":0},"started_in_background":0,"max_depth":0,"spawned_by_subagents":0,"completed":0,"failed":0,"killed":{"parent":0,"user":0,"system":0},"refused":{"depth_limit":0,"concurrency_limit":0,"budget":0},"by_type":{}},"is_error":false,"num_turns":1,"subtype":"success","api_error_status":null,"result":"# A2-S1 supplemental review (Opus, independent)\n\n**Scope:** only the appended A2-S1 collection/column mask delta and its integration into C1/C2/C7, WP1–WP8 and T21. Main A2 review sealed separately; nothing below re-raises main-packet issues.\n\n## Verdict\n\n**Ready with changes.** The reuse decision (extend `access/fields.go`'s parsed `fieldPattern`/`fieldSet` rather than add an evaluator) is correct and the tightening of C7 mask grammar before freeze is an improvement. However, adding *exclusions* inverts a safety invariant that the existing matcher depends on, and the delta asserts the required behaviour in prose without specifying the mechanism. OS-01 and OS-02 should be resolved in the documents before C1/C2/C7 freeze; the rest are pre-implementation clarifications.\n\n## Findings\n\n| ID | Sev | Dimension | Evidence | Concrete failure | Recommended action | Blocks freeze |\n|---|---|---|---|---|---|---|\n| **OS-01** | high | correctness / security (layered enforcement) | Supplement: \"`allows(path) = any(include matches path) AND NOT any(exclude matches path)`\" applied to the audited matcher `fields.go:79-100` (`if len(pattern.segments) > len(segments) { continue }`), `disallowedUpdates:215-228` (checks only `item.FieldPath()`), `disallowedPaths:189-212` (descends only `map[string]any`), `enumerable:105-123`. | Legacy allow-lists are safe because *include of a parent implies include of every descendant*. With exclusions this is false, and the existing code only ever tests the path it is handed: (a) `update {op:set, path:[\"address\"], value:{...}}` yields refusal-check path `\"address\"`; exclusion `address.internal_*` has more segments, so `continue` → no match → **excluded child is overwritten/removed and permitted**, exactly the case T21(c) claims to forbid; (b) an array-of-objects value is a leaf at its parent path, so `contacts` passes while `contacts[i].secret_email` is written/returned — the supplement says arrays are \"unsupported\" but the current code fails **open**, not closed; (c) `include:[\"address\",\"name\"] exclude:[\"address.internal_*\"]` is still `enumerable()` (no `*` in first segments), so the projection fast path can hand the whole `address` object to the adapter/caller. | Make normative in C1: an exclusion-bearing set requires **affected-path expansion** — the union of candidate-image leaf paths, pre-image leaf paths under every touched parent, and returned/projected leaf paths — before `allows` is consulted; any composite value that cannot be fully expanded to leaves (arrays, opaque provider values, unavailable pre-image, remote hop carrying only changed paths) is `ACL_ENFORCEMENT_UNSUPPORTED`, not allowed. Require `enumerable()` to return `ok=false` whenever any exclusion exists that is not a whole first-segment literal subtractable from the name list. Add T21 rows for the DataTug→OVDB hop (parent replacement where only changed paths cross HTTP) and for array/opaque values. | **Yes** (C1 semantics) |\n| **OS-02** | medium | contract completeness / interop | Delta adds restriction `kind:field_mask`, `representation:\"mask\"`, and `ACL_COLLECTION_DENIED`; frozen `contracts/acl.schema.json` `$defs.restriction` allows only `expression|fields|reference`, `$defs.blocker.code` has no collection code, and `contracts/README.md` §5 requires reference integrity over those variants. Canonical style rules in `contracts/README.md` say \"semantic arrays retain order except defined sets\" — masks are not classified. | WP1 golden vectors and generated TS/Go DTOs cannot be produced: a `mask` restriction fails the pinned schema, and two serializers may emit `include:[\"b\",\"a\"]` vs `[\"a\",\"b\"]` and both claim canonical idempotence, breaking the byte-identical guarantee in C1 and the WP2a round-trip gate. | Publish the exact `$defs` deltas with the supplement (mask restriction variant; `field_mask` added to the reference `kind` enum; `ACL_COLLECTION_DENIED` in blocker codes; `collectionMask`/`fieldMask` in `$defs.policy`/`$defs.rule` with `fieldMask` mutually exclusive with `fields`). State explicitly whether mask arrays are order-preserving lists or sorted deduplicated sets, and add a canonical-bytes fixture. | **Yes** (C2/C7 shapes) |\n| **OS-03** | medium | security / privacy disclosure | Delta: \"Public policy text intentionally discloses its mask literals\"; C2 restriction disclosure rules (04, 15) treat `field_allowlist` as ordinarily disclosable; C6 (15) requires filtered schema discovery to expose \"only accessible collection/field names\". | An allow-list names what you *can* see; an exclusion mask names what you *cannot*. Returning a `field_mask` restriction with `exclude:[\"secret_*\",\"salary\",\"address.internal_*\"]` to an ordinary caller for their own operation leaks hidden column names that C6 schema filtering deliberately withholds — a new disclosure channel not present in the main packet. | Add to 10/15: exclusion patterns are disclosable only to callers with policy-read on that policy; otherwise emit the include projection only, or coalesce to a `reference` (`kind:field_mask`, `omissionReason:not_authorized`). Add a T21(g) assertion that ordinary diagnostics never reveal exclude literals. | No |\n| **OS-04** | medium | correctness / MVP scope | Delta: nested/collection-group requests \"whose touched collections cannot be fully resolved are unsupported under this profile, never treated as matching `*`\". No code or reduction outcome is assigned. | C2 reduction treats definitive deny and unsupported differently (`deny` dominates; unsupported → `indeterminate`/422 `authorization_unsupported`). Two owners can classify the same nested path differently, producing non-deterministic effective results and breaking T04 permutation invariance. Additionally, any existing deployment using `/spaces/{spaceID}/customers` becomes wholly unusable the moment a `collectionMask` policy is added — a migration hazard worth stating. | Specify: unresolvable/nested collection under a present `collectionMask` = `ACL_ENFORCEMENT_UNSUPPORTED`, scope `operation`, no execution; `ACL_COLLECTION_DENIED` is reserved for a resolved name failing the gate. Document the nested-path limitation in 03 and WP9 migration notes. | No |\n| **OS-05** | medium | correctness / DataTug UX | Delta: \"A new column matching `include:[\"*\"]` is allowed unless excluded; explicit legacy field lists continue to deny unlisted new fields. The UI must show this difference.\" D04/03 promise \"New fields stay denied by that explicit list\" for the UI deny-column action. | The product now has two opposite defaults for the same admin intent. If the UI's \"deny column\" emits `fieldMask{include:[\"*\"],exclude:[col]}`, adding `credit_score` to the schema later silently exposes it — an authorization regression caused by a schema change, contradicting the packet's stated guarantee. | State normatively that the UI deny-column action continues to emit a legacy `fields` allow-list; exclusion masks are an explicit, separately labelled authoring mode with a persisted warning. Extend T21(d) to assert **both** representations under the same new-field scenario. | No |\n| **OS-06** | low-medium | contract precision | Delta: \"All new name masks (procedure base names, collections, individual column segments) use the same bounded existing segment grammar\"; but `fields.go:44-56` shows field patterns are **dotted, multi-segment, with a `.*` subtree suffix**, while collection and callable names are single-token and must reject `.`. Frozen `executionGate` pattern `^[^./\\\\\\x00-\\x1f?\\[\\]]+$` still admits interior/multiple `*` (e.g. `U*er_*`), which the supplement now forbids. | A validator written from this sentence either rejects `address.*` as a field mask or accepts `sys.*` as a collection mask; the frozen schema still accepts procedure masks the supplement says must be rejected. | Define two grammars explicitly: `nameMask` (single segment, no `.`; literal | `*` | `prefix*` | `*suffix`) for collections and callables, and `fieldPattern` (existing dotted grammar incl. terminal `.*`) for columns. Tighten the schema `pattern` (or record it as a semantic-validator obligation in `contracts/README.md` §1). | No |\n| **OS-07** | low | reuse / compatibility | `fields.go:21-25`: `pattern.subtree` is parsed but never consulted by `allows` — `address` and `address.*` behave identically today. The delta asserts \"`address.*` selects descendants and current parent-prefix behavior\". | If exclusion handling is implemented by newly honouring `subtree`, legacy `fields:[\"address\"]` semantics change (currently covers descendants). Silent behaviour change in shipped policies. | State in WP2a acceptance that `subtree` remains non-semantic for inclusion, and add a legacy differential vector asserting `fields:[\"address\"]` and `fields:[\"address.*\"]` still decide identically. | No |\n| **OS-08** | low | testability | T21 lists scenarios but not the owner/hop matrix; WP8 wording (\"actual protected queries and updates\") does not bind T21 to the three-owner intersection proof used by T03/T07. | A green T21 run against InGitDB alone would leave the OVDB-mount and DataTug-remote paths (where OS-01(a) is most likely) untested, and the \"no union defeats an exclusion\" case (e) is cheap to satisfy with a single-policy fixture. | Bind each T21 sub-case to owners (direct adapter, OVDB mount, DataTug remote) and require case (e) to use two mandatory policies at different owners plus two alternatives inside one policy. | No |\n\n## Human decisions (choices, not bugs)\n\n1. **New-column default** (OS-05): allow-list-denies-new vs mask-allows-new. Both are legitimate; the user must choose which the UI produces by default.\n2. **Exclusion-mask literal disclosure** (OS-03): whether public policy text may name hidden columns to ordinary callers, given C6 filtered schema. This is a deliberate trade-off between the approved public-by-default direction and column-name confidentiality.\n3. **Nested-collection behaviour** (OS-04): fail-closed-unsupported vs deferring `collectionMask` until nested paths are resolvable.\n\n## Reuse assessment — worth preserving\n\n- Extending the audited `fieldPattern`/`fieldSet` with exclusions and reusing `segmentMatches` (`fields.go:63-74`) instead of a schema-snapshot expansion or a second evaluator.\n- Keeping `fields` and `fieldMask` mutually exclusive with no silent reserialization; legacy documents keep exact meaning.\n- Treating `collectionMask` as a conjunctive gate rather than a path selector with invented specificity — it does not disturb `matchingRules` precedence (`policy.go:353-375`).\n\n## Limits of this review\n\nAssessed only the supplement text plus the `fields.go:1-241` excerpt at the stated SHA. `redactMap` is truncated at line 240, so its array handling and post-projection ordering are inferred, not verified; the query projection call sites of `enumerable()` were not in evidence, so OS-01(c) is an inference from the function contract (\"callers must fall back to redaction\"), not an observed bypass. No tests were run and no repository was inspected beyond the provided excerpts.","ttft_ms":94386,"type":"result","duration_ms":150891,"uuid":"9acf1ca7-ecb1-40b7-a4a3-ce515f4b1ad3","ttft_stream_ms":1860,"time_to_request_ms":27,"first_content_frame_ms":2294,"queued_turn_count":0}
